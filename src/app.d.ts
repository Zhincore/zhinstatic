/// <reference types="@sveltejs/kit" />

declare namespace App {
  interface Locals {
    darkmode?: boolean;
  }

  // interface Platform {}

  // interface PrivateEnv {}

  // interface PublicEnv {}
}
